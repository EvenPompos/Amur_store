<Page x:Class="Amur_store.Views.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Каталог товаров"
      Loaded="Page_Loaded">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Каталог товаров" FontSize="24" FontWeight="Bold"/>
            <TextBlock x:Name="CatalogInfoText" Text="Загрузка..." Margin="20,0,0,0" VerticalAlignment="Bottom" Foreground="Gray"/>
        </StackPanel>

        <DataGrid x:Name="ProductsDataGrid" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  CanUserAddRows="False"
                  Margin="10" 
                  BorderThickness="1" BorderBrush="#DDDDDD">

            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Фото" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding ImagePath}" Height="50" Stretch="Uniform"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Название" Binding="{Binding ProductName}" Width="*" FontWeight="SemiBold"/>

                <DataGridTextColumn Header="Производитель" Binding="{Binding ManufacturerName}" Width="140"/>

                <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat={}{0:N0} ₽}" Width="100" FontWeight="Bold"/>

                <DataGridTemplateColumn Header="В корзину" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="+" 
                                    FontSize="20" FontWeight="Bold"
                                    Background="#4CAF50" Foreground="White"
                                    Width="50" Height="35"
                                    Cursor="Hand"
                                    Click="AddToCart_Click"
                                    Tag="{Binding ProductID}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>